---

import SectionLayout from "@/components/Layout/SectionLayout.astro";
import SectionTitle from "@/components/UI/SectionTitle.astro";
import { WorksCardSlider } from "@/components/Works/WorksCardSlider";
import BackgroundSection from '@/components/Layout/BackgroundSection.astro';

import worksBackgroundImage from '@/assets/images/works-background-img.jpg'
import { type WorkForClient } from "@/data/works";
import SectionCtaLink from "@/components/UI/SectionCtaLink.astro";
interface Props {
    works: WorkForClient[];
}

const {works} = Astro.props;
---


<BackgroundSection image={worksBackgroundImage} alt="" showOverlay={true}
>
    <SectionLayout>
        <div class="z-20 flex-1 flex flex-col justify-between max-h-[900px] my-auto gap-10" slot="sectionContent">
        <!-- 縦の flex ＋ justify-between に変更 -->
            <!-- 上側：タイトル -->
            <SectionTitle title="WORKS">
            Step into my world through selected works.<br>
            —or an intimate collection of moments across genres.
            </SectionTitle>
            <!-- 下側：案内文＋スライダーをラッパーでひとまとめ -->
            <div>
            <!-- 上下スクロール案内 -->
            <p class="text-[var(--color-white)] text-sm mb-2">
                上下スクロールで作品が流れます
            </p>
            <!-- カードスライダー用ラッパー（横スクロール） -->
                <WorksCardSlider works={works} client:load/>
            </div>
        </div>
        <SectionCtaLink
        slot="sectionCta"
        href="/works"
        label="Works archive"
        />
    </SectionLayout>
</BackgroundSection>