---
const { className } = Astro.props
---


<div class="min-h-screen bg-black text-[var(--color-white)]">
    <div class={`mx-auto px-4 md:px-8 lg:px-12 pb-20 pt-30 space-y-10 ${className ?? "max-w-5xl"}`}>
        <slot/>
    </div>
</div>